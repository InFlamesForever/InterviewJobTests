server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ultraplex
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
