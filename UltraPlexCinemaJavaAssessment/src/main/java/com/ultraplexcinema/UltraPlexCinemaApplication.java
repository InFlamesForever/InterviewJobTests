package com.ultraplexcinema;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Incredibly basic application, no need for additional beans or config to do anything in the spec.
 */
@SpringBootApplication
public class UltraPlexCinemaApplication {
    public static void main(String[] args) {
        SpringApplication.run(UltraPlexCinemaApplication.class, args);
    }
}
